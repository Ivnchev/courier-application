import { NewsSearchPipe } from './news-search.pipe';

describe('NewsSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new NewsSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
