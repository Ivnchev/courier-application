import { SupportQuestionSearchPipe } from './support-question-search.pipe';

describe('SupportQuestionSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new SupportQuestionSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
