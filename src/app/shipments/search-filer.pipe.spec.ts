import { SearchFilerPipe } from './search-filer.pipe';

describe('SearchFilerPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFilerPipe();
    expect(pipe).toBeTruthy();
  });
});
