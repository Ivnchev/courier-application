import { SearchClaimPipe } from './search-claim.pipe';

describe('SearchClaimPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchClaimPipe();
    expect(pipe).toBeTruthy();
  });
});
